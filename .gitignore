node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment — never commit secrets
.env
.env.local
.env.production

# Local uploads — use R2 in production
uploads/

# Replit internal state
.local/
.replit

# Local dev artifacts
temp_repo/
"#"