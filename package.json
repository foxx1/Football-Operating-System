{
  "name": "360fos",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm run dev --workspace=apps/api",
    "dev:app": "npm run dev --workspace=apps/app",
    "build": "npm run build --workspace=apps/api && npm run build --workspace=apps/app",
    "check": "npm run check --workspace=apps/api && npm run check --workspace=apps/app",
    "lint": "npm run lint --workspace=apps/api && npm run lint --workspace=apps/app",
    "test": "npm test --workspace=apps/api && npm test --workspace=apps/app",
    "validate": "npm run validate --workspace=apps/api && npm run validate --workspace=apps/app"
  },
  "overrides": {
    "@radix-ui/react-direction": "^1.1.2"
  }
}
