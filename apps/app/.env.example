# Leave unset for local dev — relative /api and /uploads requests are
# proxied to apps/api's dev server (see vite.config.ts). Set for prod builds
# since apps/app and apps/api deploy to separate origins.
VITE_API_URL=https://api.360fos.com
